dist: trusty
language: node_js
node_js:
  - 'lts/erbium'
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
cache:
  directories:
    - ./node_modules
install:
  - npm install
script:
  - npm run test-ci
  - npm run build-ci